# bump-recorder-maps

Offline basemap data for the Bump Recorder app.

`serbia.pmtiles` is a Protomaps vector extract of Serbia (z0-14), derived from OpenStreetMap data (ODbL) and Natural Earth. Streamed by the app via HTTP range requests.
